object ThreeNumberSum{

  def main(args: Array[String]): Unit = {
      val array: Array(12,3,1,2,-6,5,-8,6);
      val targetSum = 0;
      println(SumThreeNumber(array = array,targetsum = targetSum))
  }

  def SumThreeNumber(array: Array[Int], targetsum:Int) : Unit = {
    var result = Array[Int];
    var c = 0;
    for (i <- array.length){
       print(i);
    }
  }

}